# Get metrics from the object

Get metrics from the object

## Usage

``` r
get_metrics(object, ...)

# S3 method for class 'luz_module_fitted'
get_metrics(object, ...)
```

## Arguments

- object:

  The object to query for metrics.

- ...:

  Currently unused.

## Value

A data.frame containing the metric values.

## Methods (by class)

- `get_metrics(luz_module_fitted)`: Extract metrics from a luz fitted
  model.
